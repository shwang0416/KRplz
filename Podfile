# Uncomment the next line to define a global platform for your project
# platform :macOS, '10.11'

target 'CaptureSample' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'Differ'
  # Pods for CaptureSample

 target 'CaptureSampleTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'Differ'
  end

  target 'CaptureSampleUITests' do
    # Pods for testing
    pod 'Differ'
  end

end
